name: 'commercial_analytics'
version: '1.0.0'
config-version: 2

profile: 'commercial_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  commercial_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table